/* ============================================================
   ЛОКАЛЬНЫЙ КОНФИГ (необязательный)

   Как пользоваться:
   1. Скопируй этот файл и назови копию: config.local.js
   2. Впиши свой API-ключ ниже.
   3. Готово — приложение подхватит настройки само, без ручного
      ввода в ⚙️ Настройках.

   Файл config.local.js НЕ попадает в git (он в .gitignore),
   поэтому твой ключ останется только у тебя на компьютере.
   ============================================================ */

window.MET_CONFIG = {
  // "openai" — облачный API (Groq / Gemini / OpenAI), "ollama" — локально
  provider: 'openai',

  // --- Настройки облачного API (Groq по умолчанию) ---
  openaiUrl: 'https://api.groq.com/openai/v1',
  openaiKey: 'ВСТАВЬ_СВОЙ_КЛЮЧ_СЮДА', // например: gsk_...
  openaiModel: 'llama-3.3-70b-versatile',

  // --- Настройки Ollama (если provider: 'ollama') ---
  // ollamaUrl: 'http://localhost:11434',
  // ollamaModel: 'llama3.2',
};
